return {
    yaml = {
        -- other settings. note this overrides the lspconfig defaults.
        schemaStore = {
            enable = true
        },
        format = {
            enable = true,
        },
        editor = {
            autoIndent = "advanced",
        },
    },
}
